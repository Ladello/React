import { BrowserRouter } from 'react-router-dom';
import MinhasRotas from "./minhasRotas"; // verifique se o nome do arquivo está exatamente assim!

function App() {
  return (
    <BrowserRouter>
      <MinhasRotas />
    </BrowserRouter>
  );
}

export default App;
